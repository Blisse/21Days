<Page
    x:Class="HappyBetterWP81.Pages.SelectDatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyBetterWP81.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding SelectDatePage, Source={StaticResource Locator}}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid>
            <FlyoutPresenter Loaded="FlyoutPresenter_Loaded">
                <DatePickerFlyout x:Name="DatePickerFlyout"
                                  MaxYear="{Binding MaximumYearOffset}"
                                  MinYear="{Binding MinimumYearOffset}"
                                  DatePicked="DatePickerFlyout_OnDatePicked">
                </DatePickerFlyout>
            </FlyoutPresenter>
        </Grid>
    </Grid>
</Page>
